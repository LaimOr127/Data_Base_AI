# ✅ Установка DIGroup на сервер завершена успешно!

## 🌐 Доступ к сервисам

### Основной сервис (База знаний)
- **URL:** http://85.198.99.150:6806
- **Статус:** ✅ Работает
- **Версия:** 3.4.2

### Мониторинг
- **Grafana:** http://85.198.99.150:3000
  - Логин: `admin`
  - Пароль: `digroup2026`
- **Prometheus:** http://85.198.99.150:9090

### ИИ (Ollama)
- **URL:** http://85.198.99.150:11434

## 📋 Информация о сервере

- **IP адрес:** 85.198.99.150
- **Пользователь:** root
- **Пароль:** !K5kUHw6Hc0%
- **ОС:** Ubuntu 24.04
- **Директория установки:** `/opt/digroup`

## 👥 Пользователи

Загружено **131 пользователь** из файла `users_db/users.csv`.

Для входа используйте логин и пароль из CSV файла (например, `sha` / `sha123`).

## 🔧 Управление сервисом

### Подключение к серверу
```bash
ssh root@85.198.99.150
# Пароль: !K5kUHw6Hc0%
```

### Полезные команды

```bash
# Переход в директорию проекта
cd /opt/digroup

# Просмотр статуса контейнеров
docker compose ps

# Просмотр логов
docker compose logs -f digroup

# Перезапуск сервиса
docker compose restart

# Остановка сервиса
docker compose down

# Запуск сервиса
docker compose up -d

# Проверка доступности
curl http://localhost:6806/api/system/version
```

## 📝 Что было сделано

1. ✅ Сервер очищен от старых данных
2. ✅ Установлен Docker и Docker Compose
3. ✅ Проект склонирован с GitHub
4. ✅ Настроены пользователи (131 аккаунт)
5. ✅ Собран и запущен Docker образ
6. ✅ Настроен файрволл (порты 6806, 6808 открыты)
7. ✅ Все сервисы запущены и работают

## 🔒 Безопасность

- Порты открыты для внешнего доступа
- Пользователи защищены хешированными паролями (bcrypt)
- Код доступа хранится в `.env` файле

## 📦 Резервное копирование

Резервные копии сохраняются в `/opt/digroup/backups/`

Для создания резервной копии:
```bash
cd /opt/digroup
./backup.sh
```

## 🔄 Обновление

Для обновления проекта:
```bash
cd /opt/digroup
git pull
docker compose build --no-cache digroup
docker compose up -d
```

## ⚠️ Важные заметки

1. **Пароли пользователей** хранятся в открытом виде в `users_db/users.csv`
2. **Код доступа** находится в `/opt/digroup/.env` (ACCESS_AUTH_CODE)
3. **Конфигурация** находится в `/opt/digroup/workspace/conf/conf.json`

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker compose logs -f digroup`
2. Проверьте статус: `docker compose ps`
3. Перезапустите: `docker compose restart`

---

**Дата установки:** 20 января 2026
**Версия:** 3.4.2
